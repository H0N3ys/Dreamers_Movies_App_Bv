import 'package:flutter/material.dart';

class AppColors {
  // Fondo principal azul oscuro de la app
  static const Color primaryColor = Color(0xFF152A72);
  
  // Color de texto claro
  static const Color textColor = Color(0xFFF3F4F6);
  
  // Acento para menús, tarjetas o fondos de botones (un tono oscuro)
  static const Color accentColor = Color(0xFF1C2434); 
  
  // Azul brillante secundario para botones destacados o iconos activos
  static const Color secondaryColor = Color(0xFF3B82F6); 
}